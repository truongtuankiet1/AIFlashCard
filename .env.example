# Database
DATABASE_URL=postgresql://username:password@localhost:5432/vocabcards

# OpenAI API (System API Key - optional)
# If not provided, users must configure their own key in settings
OPENAI_API_KEY=sk-xxx

# NextAuth Configuration
NEXTAUTH_SECRET=your-secret-random-string-change-this-in-production
NEXTAUTH_URL=http://localhost:3000

# Encryption
# Used to encrypt user API keys in database
# Generate a strong random string for production
ENCRYPTION_KEY=your-encryption-key-change-this-in-production

# Optional: Redis (for caching and rate limiting)
# REDIS_URL=redis://localhost:6379

# Optional: Monitoring
# SENTRY_DSN=https://...

# `openssl rand -base64 32`
AUTH_SECRET=
AUTH_URL=http://localhost:3000/api/auth